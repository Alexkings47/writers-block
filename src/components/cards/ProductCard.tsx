import React from 'react'
import styled from 'styled-components'

const ProductCard = () => {
  return (
    <StyledDiv>ProductCard</StyledDiv>
  )
}

export default ProductCard
const StyledDiv = styled.div`
    
`